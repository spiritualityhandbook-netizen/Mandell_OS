# 06 - Emoji Manifest Layer

Goal: Integrate `lexicon/emoji_manifest.json` into runtime and AI bridge.

Tasks:
- ensure lexer loads emoji manifest dynamically (done)
- add emoji lookup helper in `engine/terminal_architect.js`
- create tests verifying emoji token -> semantic mapping
