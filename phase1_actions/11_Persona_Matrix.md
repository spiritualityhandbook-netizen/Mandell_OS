# 11 - Persona Matrix

Goal: Implement persona archetypes and lock `THE_SOVEREIGN` by default.

Tasks:
- store persona manifests under `persona/` (create if missing)
- lock default persona to SOVEREIGN; add OwnerKey override
- expose persona selection API for dashboard
